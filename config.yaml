name: "Stream mqtt from Enphase Envoy"
description: "Converts Envoy Json to mqtt"
version: "1.0.9.9"
slug: "envoy_to_mqtt_json"
init: false
arch:
  - aarch64
  - amd64
  - armhf
  - armv7
  - i386
options:
  MQTT_HOST: ip.address.of.homeasistant
  MQTT_USER: mqtt.user
  MQTT_PASSWORD: mqtt.password
  MQTT_PORT: 1883
  ENVOY_HOST: envoy.local
  ENVOY_USER: secret
  ENVOY_USER_PASS: secret
  ENVOY_PASSWORD: leave empty to generate
  TOPIC_FREEDS: Inverter/GridWatts
  ENVOY_TOKEN: leave empty to generate
schema:
  MQTT_HOST: str
  MQTT_USER: str
  MQTT_PASSWORD: password
  MQTT_PORT: int
  ENVOY_HOST: str
  ENVOY_USER: email
  ENVOY_USER_PASS: password
  ENVOY_PASSWORD: "str?"  
  TOPIC_FREEDS: "str?"
  ENVOY_TOKEN: "str?"
